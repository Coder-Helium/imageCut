seed: 42
device: auto
output_dir: runs/daccnet_smoke
epochs: 1
batch_size: 2
num_workers: 0
lr: 0.0003
weight_decay: 0.0001
model:
  num_queries: 4
  width: 24
  hidden_dim: 96
  num_decoder_layers: 1
  nhead: 4
train_dataset:
  jsonl_path: runs/dacc_smoke_data/metadata/all.jsonl
  image_size: 160
  max_records: null
  max_targets: 4
  min_positive_score: 3.0
val_dataset:
  jsonl_path: runs/dacc_smoke_data/metadata/all.jsonl
  image_size: 160
  max_records: null
  max_targets: 4
  min_positive_score: 3.0
loss_weights:
  box_l1: 2.0
  giou: 2.0
  score: 0.5
  action: 0.5
  issue: 0.3
  neg_score: 0.1

